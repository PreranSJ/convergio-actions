name: Build-Deploy-Stage

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  Build-Test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [24]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Npm install (frontend)
        working-directory: frontend
        run: npm ci

      - name: Run Tests (frontend)
        working-directory: frontend
        run: npm test -- --passWithNoTests || true

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      # ---- AWS steps commented out for now - uncomment when needed
      # - name: Sonarqube Scan (only if secrets present)
      #   if: secrets.SONAR_TOKEN != '' && secrets.SONAR_HOST_URL != ''
      #   uses: SonarSource/sonarqube-scan-action@v6
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      #     SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      #
      # - name: Configure AWS Credentials
      #   uses: aws-actions/configure-aws-credentials@v2
      #   with:
      #     role-to-assume: ${{ secrets.AWS_STAGE_GITHUB_ROLE }}
      #     role-session-name: GitHub_to_AWS_via_FederatedOIDC
      #     aws-region: af-south-1
      #
      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v1
      #
      # - name: Build, tag, and push image to Amazon ECR
      #   id: build-image
      #   env:
      #     ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      #     IMAGE_TAG: latest
      #   run: |
      #     docker pull $ECR_REGISTRY/convergio-build:$IMAGE_TAG || true
      #     docker build -t $ECR_REGISTRY/convergio-stage-web:$IMAGE_TAG ./frontend
      #     docker push $ECR_REGISTRY/convergio-stage-web:$IMAGE_TAG
      #
      # - name: ECS Deployment
      #   run: |
      #     aws ecs update-service \
      #       --cluster convergio-stage \
      #       --service convergio-stage-web \
      #       --force-new-deployment
