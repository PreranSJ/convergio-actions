name: Build-Deploy-Prod

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  Build-Test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [18]

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: NPM Install (frontend)
        working-directory: frontend
        run: npm ci

      - name: Run Tests (frontend)
        working-directory: frontend
        run: npm test -- --passWithNoTests || true

      - name: Npm Build (frontend)
        working-directory: frontend
        run: npm run build

      # ---- AWS steps commented out for now - uncomment when needed
      # - name: Configure AWS Credentials
      #   uses: aws-actions/configure-aws-credentials@v5.1.0
      #   with:
      #     role-to-assume: ${{ secrets.AWS_PROD_GITHUB_ROLE }}
      #     role-session-name: GitHub_to_AWS_via_FederatedOIDC
      #     aws-region: af-south-1
      #
      # - name: Setup env (optional)
      #   run: |
      #     aws ssm get-parameters \
      #       --with-decryption \
      #       --names "${{ secrets.PROD_CONFIG }}" \
      #       --output text \
      #       --query "Parameters[*].{Value:Value}" > $PWD/.env
      #
      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v2
      #
      # - name: Build and Push Docker Image
      #   env:
      #     ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      #     IMAGE_TAG: latest
      #   run: |
      #     docker pull $ECR_REGISTRY/juniorminds-build:$IMAGE_TAG || true
      #     docker build -t $ECR_REGISTRY/juniorminds-prod-web:$IMAGE_TAG ./frontend
      #     docker push $ECR_REGISTRY/juniorminds-prod-web:$IMAGE_TAG
      #
      # Deploy:
      #   runs-on: ubuntu-latest
      #   needs: Build-Test
      #
      #   steps:
      #     - name: Configure AWS Credentials
      #       uses: aws-actions/configure-aws-credentials@v5.1.0
      #       with:
      #         role-to-assume: ${{ secrets.AWS_PROD_GITHUB_ROLE }}
      #         role-session-name: GitHub_to_AWS_via_FederatedOIDC
      #         aws-region: af-south-1
      #
      #     - name: ECS Deployment
      #       run: |
      #         aws ecs update-service \
      #           --cluster juniorminds-prod \
      #           --service juniorminds-prod-web \
      #           --force-new-deployment
      #
      #     - name: ECS Service Health
      #       run: |
      #         aws ecs wait services-stable \
      #           --cluster juniorminds-prod \
      #           --services juniorminds-prod-web